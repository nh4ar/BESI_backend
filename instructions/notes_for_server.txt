ssh -i inertiauva.pem ubuntu@18.233.6.250

cd ~/tablet-backend
# next activate the virtual environment
source env/bin/activate

# you should see (env) appended in front of your shell

cd ~/tablet-backend/besi_tablet_backend

# you can run the server two ways. First is the simple way
python manage.py runserver 127.0.0.1:8001

# then you should see the DJango console pop up and run. You can watch the HTTP GET, POST, etc requests come in


# you should run it in the background when you're ready to leave the server up and running
# repeat and run all the steps above in screen
screen

cd ~/tablet-backend
# next activate the virtual environment
source env/bin/activate

# https://stackoverflow.com/questions/1188542/django-runserver-permanent
cd ~/tablet-backend/besi_tablet_backend
python manage.py runserver 127.0.0.1:8001

# Press Ctrl+A then press d to exit from the screen

# To return back to the server screen, type
screen -r